# 2026-04-27 开发日志

## 今日目标

- 以 `claude_code_prompt_ai_resume_platform_final.md` 和 `AGENTS.md` 为参考，确认系统主流程可用，并补齐缺口。
- 修复「教育经历无法添加」以及「缺少工作/项目经历入口」等影响主流程的问题。
- 增加服务端日志留存，方便后续定位线上问题。
- 优化岗位网址抓取：避免把“加载中/安全校验/导航页”当作 JD。

## 系统检查与主流程修复

- 发现本机环境无法完成完整构建：
  - Next.js 16 需要 Node.js >= 20.9，本机为 Node 18。
  - 本机 pnpm / Prisma 生成存在权限问题。
- 在后端增加了 AI 降级路径：没有 OpenAI Key 时也能完成「岗位解析」与「简历生成」的保底流程（仅复用用户已有事实，不虚构经历）。
- 修复岗位创建与简历生成链路的缺口：创建岗位后能触发解析，简历与岗位目标关联后能触发生成，形成可编辑版本。

## 教育/经历入口与数据保存修复

- 修复教育/工作/项目相关页面的 API 地址与提交格式问题，避免生产环境请求到 `localhost` 或字段类型不兼容导致保存失败。
- 在「主档案」列表页补齐入口：教育、工作经历、项目经历、技能、证书都能直达维护页面。

## 服务端日志系统

- 新增后端文件日志落盘，保留 request/operation/error 三类信息，便于排查线上问题：
  - `apps/api/logs/app.log`
  - `apps/api/logs/operations.log`
  - `apps/api/logs/error.log`
- 记录关键节点：岗位创建/解析、简历创建/生成、发布与公开访问、PDF 下载、HTTP 错误等。

## 岗位网址抓取（第一轮）

- 在 `JobsService.resolveJdText` 增加“两阶段抓取”：
  1. 先 HTTP 抓取并转文本。
  2. 如果内容不可用，再用 Puppeteer 渲染后提取文本。
- 增加抓取相关 operation 日志，明确每次抓取是否触发了浏览器渲染、是否成功、提取文本长度等。
- 对 BOSS 直聘做了初步处理：尽量提取可见职位卡片，避免把纯导航页传给 AI。

## 验证与部署

- 生产环境在云服务器 Node 20 上构建并验证（Web 3000 / API 3001）。
- 完成基本 smoke test：创建主档案 -> 创建岗位 -> 解析 -> 生成简历 -> 发布 -> 公开访问。

