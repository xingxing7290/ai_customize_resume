# 2026-04-29 开发日志

## 今日目标

- 修复 PDF 导出中文变方框的问题。
- 保持“预览样式 = PDF 样式”的输出一致性。

## PDF 中文方框修复

- 定位原因：服务器仅有 DejaVu/Liberation 等西文字体，缺少 CJK 字体，Chromium 无法渲染中文字形导致方框。
- 服务器安装中文字体并刷新缓存：
  - `fonts-noto-cjk`
  - `fonts-noto-cjk-extra`
  - `fc-cache -fv`
- PDF CSS 字体栈调整为优先使用 `Noto Sans CJK SC`，确保 Puppeteer 生成 PDF 时正确匹配中文字体。
- 验证：生成的 PDF 中已能看到嵌入的 `NotoSansCJKsc-Regular/Bold` 字体信息，中文应不再是方框。

## 职位网址结构化抓取增强

- 新增 `apps/api/scripts/job_url_fetcher.py`，用于从招聘详情页抽取岗位名称、公司名称、薪资待遇、经验要求、学历要求、工作地点、职责、要求、技术关键词和福利信息。
- 后端 `JobsService` 改为优先调用 Python 抓取器；如果目标站点对服务器请求降级或 Python 环境缺少 Playwright，则继续使用 Node/Puppeteer 渲染文本并在 NestJS 内做结构化抽取。
- 数据库新增字段：`parsedExperienceRequirement`、`parsedEducationRequirement`，并保留 `parsedSalary`、`parsedBenefits` 等结构化结果。
- AI 岗位解析 Schema 和 Prompt 增加 `salary`、`benefits` 字段，保证 AI 兜底时也能返回薪资和福利。
- 前端岗位详情页增加经验要求、学历要求、福利待遇展示；`parsedBenefits` 已加入前端 JSON 数组自动解析。
- 服务器已执行 Prisma 迁移、重新生成 Prisma Client、构建并重启 API；Web 构建通过并保持运行。
- 验证结果：使用智联招聘详情页创建岗位目标后，接口返回 `嵌入式软件工程师`、`中科九微控股集团有限公司`、`1.5-3万`、`3-5年`、`本科`、`北京大兴区中科九微`，并成功提取 8 条要求。
